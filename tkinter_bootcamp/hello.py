from tkinter import *

# Create a root window
root = Tk()

# Create a label widget
myLabel = Label(root, text = "Hello, World!")

# Shove it into the root window
myLabel.pack()

# Put the root into the loop
root.mainloop()